# Pumping-Lemma-Visualiser
Visualise pumping lemma for regular languages. Input a language and a sample string, then demonstrate how string is divided into three parts (x, y, z). The system illustrates how repeating the substring 'y' 'n' times affects membership of the string in the language. It shows how pumping lemma is used to prove that few languages are not regular.
